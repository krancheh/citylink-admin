


export enum Path {
    LOGIN = "/login",
    SIGNUP = "/signup",
    MAIN = "/",
    ROUTE_RECORDS = "/route-records",
    ROUTES = "/routes",
    USERS = "/users",
    CITIES = "/cities",
    TICKETS = "/tickets",
}